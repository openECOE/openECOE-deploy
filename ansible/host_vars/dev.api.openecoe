---
# Organization settings
organization: "DEV UMH"
domain: dev.openecoe.umh.es
routes:
  protocol: "http"
  webui:
    url: "{{domain}}"
    port: 80
  api:
    url: "api.{{domain}}"
    port: 80
  chrono:
    url: "chrono.{{domain}}"
    port: 6080

hostname: openecoe-api-dev
app_fqdn: "{{routes.api.url}}"
app_port: "{{routes.api.port}}"

chrono_route: "{{routes.protocol}}://{{routes.chrono.url}}:{{routes.chrono.port}}"